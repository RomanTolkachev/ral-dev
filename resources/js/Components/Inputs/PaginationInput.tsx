import React, {FunctionComponent} from 'react';

interface IProps {
    className?: string
}

export const PaginationInput: FunctionComponent<IProps> = ({className}) => {
    return (
        <div className={`${className}`}>

        </div>
    );
};
